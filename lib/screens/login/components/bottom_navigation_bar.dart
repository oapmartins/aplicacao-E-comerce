import 'package:flutter/material.dart';

class BottomNavigationVar extends StatelessWidget {
  const BottomNavigationVar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
